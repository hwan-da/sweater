<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.sweater.model.dao.BoardDao">
	<select id="selectAll" resultType="Board">
		SELECT *
		FROM board
	</select>
	
	<select id="selectOne" resultType="Board" parameterType="int">
		SELECT *
		FROM board
		WHERE board_id = #{boardId}
	</select>
	
	<select id="getBoardByNickname" resultType="Board" parameterType="string">
		SELECT *
		FROM board
		WHERE writer = #{nickname}
	</select>
	
	<select id="getBoardByTag" resultType="Board" parameterType="string">
		SELECT *
		FROM board
		WHERE tag = #{tag}
	</select>
	
	<select id="getBoardByRegion" resultType="Board" parameterType="string">
		SELECT *
		FROM board
		WHERE region = #{region}
	</select>
	
	<select id="getBoardByHeart" resultType="Board" parameterType="int">
		SELECT *
		FROM board
		WHERE board_id = #{boardId}
	</select>
	
	<select id="getBoardIdByHeart" resultType="int" parameterType="string">
		SELECT board_id
		FROM board_heart
		WHERE user_id = #{userId}
	</select>
	
	<insert id="insertBoard" parameterType="Board">
		INSERT INTO board (region, tag, writer, title, header, start_date, end_date, content, media, trail, hiking_time, hiking_dist)
		VALUES(#{region}, #{tag}, #{writer}, #{title}, #{header}, #{startDate}, #{endDate}, #{content}, #{media}, #{trail}, #{hikingTime}, #{hikingDist})
	</insert>
	
	<update id="updateBoard" parameterType="Board">
	    UPDATE board
	    SET title = #{title}, 
	        header = #{header}, 
	        start_date = #{startDate}, 
	        end_date = #{endDate}, 
	        content = #{content}
	    WHERE board_id = #{boardId}
	</update>
	
	<delete id="deleteBoard" parameterType="int">
		DELETE 
		FROM board
		WHERE board_id = #{boardId}
	</delete>
	
	<select id="isHeart" parameterType="hashMap">
		SELECT count(*)
		FROM board_heart
		WHERE user_id = #{userId} AND board_id = #{boardId}
	</select>
	
	<update id="heartUp" parameterType="int">
		UPDATE board b
		JOIN (SELECT heart FROM board WHERE board_id = #{boardId}) AS sub
		ON b.board_id = #{boardId}
		SET b.heart = sub.heart + 1
		WHERE b.board_id = #{boardId}
	</update>
	
	<update id="heartDown" parameterType="int">
		UPDATE board b
		JOIN (SELECT heart FROM board WHERE board_id = #{boardId}) AS sub
		ON b.board_id = #{boardId}
		SET b.heart = sub.heart - 1
		WHERE b.board_id = #{boardId}
	</update>
	
	<insert id="insertHeart" parameterType="hashMap">
		INSERT INTO board_heart
		VALUES(#{userId}, #{boardId})
	</insert>
	
	<delete id="deleteHeart" parameterType="hashMap">
		DELETE
		FROM board_heart
		WHERE user_id = #{userId} AND board_id = #{boardId}
	</delete>
	
	<select id="checkHeart" resultType="int" parameterType="hashMap">
		SELECT COUNT(*)
		FROM board_heart
		WHERE user_id = #{userId} AND board_id = #{boardId}
	</select>
	
</mapper>